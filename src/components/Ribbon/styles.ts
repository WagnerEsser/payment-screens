import { Box } from '@mui/material'
import styled from 'styled-components'

export const RibbonWrapper = styled(Box)`
  clip-path: polygon(0 0, 100% 0, 95% 50%, 100% 100%, 0% 100%);
  border-radius: 3px;
`
