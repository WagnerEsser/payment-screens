import { Box } from "@mui/material";
import Header from "../../components/Header";

const PixScreen = (): JSX.Element => (
  <Box>
    <Header title="João, como você quer pagar?" />
  </Box>
);

export default PixScreen;
